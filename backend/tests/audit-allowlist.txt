# Accepted findings for scripts/audit-tests.py.
#
# Format: <path-relative-to-this-dir>::<function>::<category>  # reason
#
# THE CATEGORY IS REQUIRED. An entry keyed only by test would exempt that test from every
# detector at once — which is exactly what happened: a `failure-masking` exemption for
# test_coexistence_under_simulated_cap silently granted it a `no-assertion` pass too.
#
# A REASON IS REQUIRED. An entry is a deliberate, reviewable decision — not a parking space.
# Prefer fixing the test. Never add an entry to restore a green run.
#
# TWO KINDS OF ENTRY, and the difference is load-bearing:
#
#   ACCEPTED — the pattern is correct here, and the reason says why. No prefix.
#   BACKLOG  — the finding is real and is DEFERRED WORK. The reason starts with `BACKLOG`,
#              and the auditor counts and prints these separately on every single run:
#
#                  166 finding(s) are DEFERRED WORK, not accepted patterns.
#
#              so a green gate can never be mistaken for a clean tree. This is what let the
#              gate be installed (pre-commit + CI) in the same change that added seven new
#              detectors, without weakening one assertion to get there.
#
# The auditor FAILS on a stale entry — one whose finding no longer exists. So this file can
# only shrink: fix a test, delete its line. An exemption nobody can forget to remove.

# ---------------------------------------------------------------- ACCEPTED PATTERNS
# Read, and correct as written. No BACKLOG marker.

test_selective_reprocess.py::auth_token::failure-masking  # narrow requests.ConnectionError only, on an integration-marked suite that talks to the live stack over HTTP by design; the gate runs it with the stack up, and the skip message names the missing dependency rather than hiding a failure
integration/test_diarization_perf_gates.py::test_coexistence_under_simulated_cap::failure-masking  # narrowed from a bare `except Exception` to (RuntimeError, AssertionError, AttributeError) around torch.cuda.get_device_properties only; a gpu-marked test skipping on an absent or unusable device is the intended gate, and any other exception now propagates
unit/test_auth_policy_source_of_truth.py::test_policy_runs_even_when_the_env_flag_is_false::mock-heavy  # six patch.object calls are the collaborators of confirm_password_reset (settings flag, validator, audit logger, history check, history writer, token revoker); the test exists to prove the DB-resolved policy is consulted even with PASSWORD_POLICY_ENABLED=False, and each collaborator would otherwise hit the DB or the audit sink. Not reducible without turning it into an integration test of five other subsystems.
unit/test_handler_and_ws_hardening.py::test_hardened_refuses_wildcard_cors_with_credentials::mock-heavy  # seven monkeypatch.setattr calls CONSTRUCT the production configuration under test — _validate_production_secrets refuses to boot on any one of JWT_SECRET_KEY/ENCRYPTION_KEY/REDIS_PASSWORD/DEBUG/TESTING being unset, so all of them must be valid for the assertion to be about CORS specifically. The assertion itself is a real pytest.raises(ValueError, match='CORS').
unit/test_watch_fs_event_supervisor.py::test_load_plans_records_an_error_for_an_unresolvable_path::mock-heavy  # the watch settings are DB-backed SystemSettings read through five separate accessors, so five monkeypatches ARE the configuration; the remaining two replace session_scope and WATCH_FOLDER_PATH. The assertions are on the returned plans/gate and the published status, not on any mock.

# ------------------------------------------------------------------- DEFERRED WORK
# Real findings in files this change did not own. Each is COUNTED AND PRINTED as
# backlog on every run. Delete the line when you fix the test.

# --- permissive-status (3)
e2e/test_auth_buttons.py::test_pki_button_click_attempts_auth::permissive-status  # BACKLOG(#431): `status_code in (...)` accepts success AND refusal. Needs the one status the contract actually specifies. UNTRIAGED — not read as part of the detector-landing change.
e2e/test_media_download.py::test_stream_url_is_presigned_and_supports_range::permissive-status  # BACKLOG(#431): `status_code in (...)` accepts success AND refusal. Needs the one status the contract actually specifies. UNTRIAGED — not read as part of the detector-landing change.
e2e/test_media_download.py::test_prepare_download_audio_mp3_yields_presigned_url::permissive-status  # BACKLOG(#431): `status_code in (...)` accepts success AND refusal. Needs the one status the contract actually specifies. UNTRIAGED — not read as part of the detector-landing change.

# --- negated-status (8)
api/test_files_streaming.py::test_thumbnail_public_file_no_auth_passes_authz::negated-status  # BACKLOG(#431): `status_code != X` also passes on 500. Needs the exact expected status (the seven in api/endpoints/test_permissions.py were fixed this way). UNTRIAGED.
test_admin_endpoints.py::test_super_admin_can_export_audit_logs::negated-status  # BACKLOG(#431): `status_code != X` also passes on 500. Needs the exact expected status (the seven in api/endpoints/test_permissions.py were fixed this way). UNTRIAGED.
test_cloud_seams_capabilities.py::test_community_router_reachable::negated-status  # BACKLOG(#431): `status_code != X` also passes on 500. Needs the exact expected status (the seven in api/endpoints/test_permissions.py were fixed this way). UNTRIAGED.
test_cloud_seams_capabilities.py::test_community_router_reachable::negated-status  # BACKLOG(#431): `status_code != X` also passes on 500. Needs the exact expected status (the seven in api/endpoints/test_permissions.py were fixed this way). UNTRIAGED.
test_superuser_role_invariant.py::test_super_admin_gate_allows_super_admin::negated-status  # BACKLOG(#431): `status_code != X` also passes on 500. Needs the exact expected status (the seven in api/endpoints/test_permissions.py were fixed this way). UNTRIAGED.
test_tenant_isolation.py::test_personal_file_read_surfaces_unaffected::negated-status  # BACKLOG(#431): `status_code != X` also passes on 500. Needs the exact expected status (the seven in api/endpoints/test_permissions.py were fixed this way). UNTRIAGED.
test_tenant_isolation.py::test_personal_file_read_surfaces_unaffected::negated-status  # BACKLOG(#431): `status_code != X` also passes on 500. Needs the exact expected status (the seven in api/endpoints/test_permissions.py were fixed this way). UNTRIAGED.
test_tenant_isolation.py::test_personal_file_read_surfaces_unaffected::negated-status  # BACKLOG(#431): `status_code != X` also passes on 500. Needs the exact expected status (the seven in api/endpoints/test_permissions.py were fixed this way). UNTRIAGED.

# --- status-guarded-assert (1)
e2e/test_media_download.py::test_stream_url_is_presigned_and_supports_range::status-guarded-assert  # BACKLOG(#431): the real assertion only runs `if status == N`, so a wrong status skips it. Needs an unconditional exact-status assert first. UNTRIAGED.

# --- conditional-only (5)
e2e/test_auth_buttons.py::test_navigation_works_after_login::conditional-only  # BACKLOG(#431): every assertion sits in an `if` with no `else`, so the test passes whenever the condition is false. Needs the condition asserted or a deterministic setup. UNTRIAGED.
e2e/test_gallery_actions.py::test_toolbar_does_not_overflow_header::conditional-only  # BACKLOG(#431): every assertion sits in an `if` with no `else`, so the test passes whenever the condition is false. Needs the condition asserted or a deterministic setup. UNTRIAGED.
e2e/test_speaker_gender_clusters.py::test_gender_chips_render_on_clusters::conditional-only  # BACKLOG(#431): every assertion sits in an `if` with no `else`, so the test passes whenever the condition is false. Needs the condition asserted or a deterministic setup. UNTRIAGED.
e2e/test_speaker_gender_clusters.py::test_expand_cluster_shows_gender_icons::conditional-only  # BACKLOG(#431): every assertion sits in an `if` with no `else`, so the test passes whenever the condition is false. Needs the condition asserted or a deterministic setup. UNTRIAGED.

# --- loop-only (26)
e2e/test_gallery_actions.py::test_process_dropdown_items_have_tooltips::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
e2e/test_gallery_actions.py::test_process_items_disabled_when_no_selection::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
e2e/test_gallery_actions.py::test_organize_dropdown_items_have_tooltips::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
redaction/test_apply_redactions.py::test_label_style_matches_golden::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
test_asr_settings.py::test_catalog_required_fields::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
test_fips_140_3.py::test_backup_code_hashing::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
test_search_quality.py::test_china_suppresses_airships_from_keyword::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
test_search_quality.py::test_fight_suppresses_irrelevant::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
test_search_quality.py::test_fight_no_stem_highlights::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
test_search_quality.py::test_spy_exact_precision::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
test_search_quality.py::test_keyword_mode_no_semantic::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
test_search_quality.py::test_speaker_operator_basic::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
test_search_quality.py::test_speaker_operator_filters_occurrences::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
unit/test_auth_config_validation.py::test_every_category_has_keys_to_filter_on::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
unit/test_auth_config_validation.py::test_every_field_has_a_default::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
unit/test_auth_config_validation.py::test_declared_types_agree_with_the_service_data_type_mapping::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
unit/test_crisperwhisper_normalize.py::test_words_have_leading_space_convention::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
unit/test_crisperwhisper_normalize.py::test_no_start_after_end_anywhere::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
unit/test_crisperwhisper_normalize.py::test_monotonic_non_overlapping::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
unit/test_crisperwhisper_normalize.py::test_timestamps_clamped_to_segment_bounds::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
unit/test_metadata_speaker_extractor.py::test_no_false_positive_on_plain_title::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
unit/test_metadata_speaker_extractor.py::test_confidence_boost_positive_for_match::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
unit/test_metadata_speaker_extractor.py::test_confidence_boost_negative_for_mismatch::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
unit/test_oidc_env_compat.py::test_settings_declares_every_canonical_name::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
unit/test_oidc_env_compat.py::test_the_env_config_map_only_uses_canonical_names::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.
unit/test_v382_migration_consistency.py::test_the_live_checks_carry_the_new_values::loop-only  # BACKLOG(#431): every assertion is inside a `for` over a runtime iterable, so an empty result is a green run. Needs a non-emptiness assertion outside the loop (see redaction/test_presidio.py::test_offsets_slice_back for the fix shape). UNTRIAGED.

# --- no-assertion (11)
e2e/test_a11y.py::test_gallery_home_a11y::no-assertion  # BACKLOG(#431): no assert / raises / expect / assert_* — the test can only fail by raising. Needs an explicit assertion on the outcome it is named for. UNTRIAGED.
e2e/test_a11y.py::test_speakers_page_a11y::no-assertion  # BACKLOG(#431): no assert / raises / expect / assert_* — the test can only fail by raising. Needs an explicit assertion on the outcome it is named for. UNTRIAGED.
e2e/test_a11y.py::test_baseline_is_current::no-assertion  # BACKLOG(#431): no assert / raises / expect / assert_* — the test can only fail by raising. Needs an explicit assertion on the outcome it is named for. UNTRIAGED.
e2e/test_auth_flow.py::test_registration_success::no-assertion  # BACKLOG(#431): no assert / raises / expect / assert_* — the test can only fail by raising. Needs an explicit assertion on the outcome it is named for. UNTRIAGED.
e2e/test_login.py::test_login_redirects_to_gallery::no-assertion  # BACKLOG(#431): no assert / raises / expect / assert_* — the test can only fail by raising. Needs an explicit assertion on the outcome it is named for. UNTRIAGED.
e2e/test_login.py::test_rate_limiting::no-assertion  # BACKLOG(#431): no assert / raises / expect / assert_* — the test can only fail by raising. Needs an explicit assertion on the outcome it is named for. UNTRIAGED.
e2e/test_registration.py::test_successful_registration_redirects::no-assertion  # BACKLOG(#431): no assert / raises / expect / assert_* — the test can only fail by raising. Needs an explicit assertion on the outcome it is named for. UNTRIAGED.
e2e/test_registration.py::test_can_login_after_registration::no-assertion  # BACKLOG(#431): no assert / raises / expect / assert_* — the test can only fail by raising. Needs an explicit assertion on the outcome it is named for. UNTRIAGED.
e2e/test_speaker_gender_clusters.py::test_no_chips_when_no_gender_predictions::no-assertion  # BACKLOG(#431): no assert / raises / expect / assert_* — the test can only fail by raising. Needs an explicit assertion on the outcome it is named for. UNTRIAGED.
e2e/test_speaker_gender_clusters.py::test_outlier_highlighting::no-assertion  # BACKLOG(#431): no assert / raises / expect / assert_* — the test can only fail by raising. Needs an explicit assertion on the outcome it is named for. UNTRIAGED.
e2e/test_speaker_sharing.py::test_final_screenshot::no-assertion  # BACKLOG(#431): no assert / raises / expect / assert_* — the test can only fail by raising. Needs an explicit assertion on the outcome it is named for. UNTRIAGED.

# --- weak-only (42)
e2e/test_auth_buttons.py::test_local_login_shows_gallery::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_auth_buttons.py::test_ldap_login_shows_gallery::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_auth_flow.py::test_registration_password_mismatch::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_auth_flow.py::test_registration_weak_password::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_auth_flow.py::test_registration_duplicate_email_fails::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_ldap_oidc.py::test_ldap_wrong_password_rejected::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_login.py::test_wrong_password::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_login.py::test_nonexistent_user::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_mfa.py::test_mfa_login_shows_code_prompt::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_mfa.py::test_security_settings_shows_mfa_status::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_pki.py::test_no_cert_shows_login_page::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_pki.py::test_cert_info_visible_in_settings::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_pki.py::test_cert_shows_subject_dn::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_registration.py::test_email_invalid_format::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_registration.py::test_email_missing_domain::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_registration.py::test_email_missing_at_symbol::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_registration.py::test_password_too_short::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_registration.py::test_password_mismatch::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
e2e/test_speaker_sharing.py::test_profiles_have_sharing_fields::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
integration/test_diarizer_lifecycle.py::test_model_manager_sequential_handoff::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
test_asr_settings.py::test_first_config_auto_activates::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
test_chat_gdpr_erasure.py::test_erasure_does_not_touch_another_users_conversations::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
test_chat_gdpr_erasure.py::test_org_member_erasure_leaves_the_user_row::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
test_external_llm.py::test_service_cleanup_after_failure::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
test_fips_140_3.py::test_password_auto_upgrade::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
test_mfa_security.py::test_backup_code_case_insensitive::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
test_mfa_security.py::test_backup_code_ignores_formatting::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
test_mfa_security.py::test_totp_window_zero_only_accepts_current::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
unit/test_access_token_revocation_epoch.py::test_revoking_all_sessions_stamps_the_epoch::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
unit/test_account_lifecycle.py::test_session_issue_stamps_it::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
unit/test_auth_config_validation.py::test_every_category_has_keys_to_filter_on::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
unit/test_auth_policy_source_of_truth.py::test_policy_runs_even_when_the_env_flag_is_false::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
unit/test_chat_project_scope.py::test_empty_scope_with_no_project_stays_empty::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
unit/test_chat_redactor.py::test_incomplete_detection_status_forces_the_inline_path::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
unit/test_lockout_cleanup_sweep.py::test_a_live_lockout_keeps_its_lock_expiry::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
unit/test_mock_llm_fixture.py::test_reasoning_model_reports_reasoning_content_non_streamed::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
unit/test_oidc_discovery.py::test_audience_check_skipped_when_disabled::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
unit/test_oidc_discovery.py::test_correct_issuer_is_accepted::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
unit/test_proxy_header_auth.py::test_a_correct_shared_secret_authenticates::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
unit/test_session_lifetime.py::test_first_rotation_stamps_a_legacy_row::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
unit/test_tag_service.py::test_collision_leaves_callers_pending_writes_intact::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.
unit/test_v380_migration_consistency.py::test_oidc_subject_is_still_unique::weak-only  # BACKLOG(#431): the test's only assertion is bare-truthy or `is not None`, which nearly any value satisfies. Needs an assertion that names the expected value. UNTRIAGED.

# --- mock-only (51)
test_auth_config_service.py::test_set_config_creates_new::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
test_auth_config_service.py::test_set_config_encrypts_sensitive::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
test_auth_config_service.py::test_set_config_with_request::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
test_auth_config_service.py::test_bulk_update_category::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
test_auth_config_service.py::test_bulk_update_encrypts_sensitive_keys::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
test_auth_config_service.py::test_audit_log_created_on_change::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
test_auth_config_service.py::test_migrate_from_env::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
test_chat_endpoints.py::test_a_refused_send_releases_the_stream_slot::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_audit_correlation.py::test_request_for_an_unknown_address_is_audited::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_audit_correlation.py::test_unknown_address_is_not_written_into_the_audit_record::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_audit_correlation.py::test_successful_request_is_audited::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_auth_policy_source_of_truth.py::test_policy_runs_even_when_the_env_flag_is_false::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_auth_state_degradation.py::test_degraded_requests_are_counted::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_auth_state_degradation.py::test_healthy_requests_are_not_counted::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_backup_alerts.py::test_clean_success_sends_nothing::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_backup_alerts.py::test_success_with_warnings_notifies::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_chat_limits.py::test_release_removes_only_its_own_slot::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_chat_limits.py::test_release_without_an_id_prunes_by_age_and_never_clears_the_key::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_chat_redactor.py::test_incomplete_detection_status_forces_the_inline_path::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_chat_retrieval.py::test_semantic_cache_write_is_skipped_when_caching_disabled::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_chat_retrieval.py::test_semantic_cache_is_skipped_when_disabled_in_settings::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_chat_retrieval.py::test_indexing_bumps_the_corpus_version::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_chat_retrieval.py::test_every_mode_still_carries_the_authorization_filter::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_chat_usage.py::test_usage_is_recorded_with_the_message_uuid_as_idempotency_key::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_chat_usage.py::test_use_context_is_recorded::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_chat_usage.py::test_quantity_is_tokens_not_currency::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_chat_usage.py::test_a_failed_exchange_is_still_recorded::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_llm_bedrock.py::test_temperature_is_omitted_when_unset::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_lockout_identifier_canonical.py::test_lockout_calls_use_the_canonical_key_not_the_submitted_one::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_lockout_identifier_canonical.py::test_audit_still_records_what_the_caller_typed::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_media_mirror_engine.py::test_throttle_sleeps_between_objects::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_media_mirror_engine.py::test_no_throttle_means_no_sleep::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_opensearch_auth.py::test_managed_mode_adopts_the_configured_model_without_touching_the_cluster::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_opensearch_auth.py::test_managed_mode_without_a_model_id_falls_back_to_the_active_one::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_opensearch_auth.py::test_managed_mode_with_no_model_at_all_is_a_logged_no_op::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_opensearch_orphan_sweep_guards.py::test_scheduled_run_notifies_nobody::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_opensearch_snapshot.py::test_ensure_repository_idempotent_when_present::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_opensearch_snapshot.py::test_ensure_repository_recreates_on_location_mismatch::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_password_reset_fail_closed.py::test_uses_the_transaction_aware_revocation_helper::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_postprocess.py::test_calls_all_downstream_tasks::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_postprocess.py::test_sends_search_indexing_ws_event::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_postprocess.py::test_indexing_failure_doesnt_block_others::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_postprocess.py::test_summarization_failure_doesnt_block_others::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_postprocess.py::test_speaker_attr_failure_doesnt_block_clustering::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_postprocess.py::test_passes_downstream_tasks_through::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_postprocess.py::test_passes_file_params_correctly::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_postprocess.py::test_propagates_pipeline_task_id_to_indexing::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_stale_chunk_index_recreate.py::test_index_without_meta_is_not_destroyed::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_stale_chunk_index_recreate.py::test_unreadable_dimension_aborts_before_any_delete::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_stale_chunk_index_recreate.py::test_failed_create_restores_an_index_rather_than_leaving_none::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.
unit/test_stale_chunk_index_recreate.py::test_stale_index_is_recreated_at_the_mapped_dimension::mock-only  # BACKLOG(#431): every assertion is mock bookkeeping (assert_called*/.called/.call_args) with nothing asserted about a return value or real state. Some of these are legitimate (the request IS the contract; an absent destructive call has no return value) and some are wiring tests — the split needs a per-test read. UNTRIAGED.

# --- failure-masking (3)
e2e/test_ldap_oidc.py::test_oidc_admin_login_full_flow::failure-masking  # BACKLOG(#431): `except Exception -> pytest.skip` turns a real regression into a permanent skip. Needs a narrowed exception type, or a hard failure. UNTRIAGED.
e2e/test_ldap_oidc.py::test_keycloak_regular_user_login::failure-masking  # BACKLOG(#431): `except Exception -> pytest.skip` turns a real regression into a permanent skip. Needs a narrowed exception type, or a hard failure. UNTRIAGED.
e2e/test_ldap_oidc.py::test_keycloak_wrong_credentials_rejected::failure-masking  # BACKLOG(#431): `except Exception -> pytest.skip` turns a real regression into a permanent skip. Needs a narrowed exception type, or a hard failure. UNTRIAGED.

# --- error-swallowed (13)
api/test_files_prepare_complete_upload.py::test_prepare_then_complete_round_trip::error-swallowed  # BACKLOG(#431): `except ...: pass`/return/log inside a test body — the assertions in the `try` silently stop running and nobody is told. Needs the exception to propagate or be asserted on. UNTRIAGED.
e2e/test_ldap_oidc.py::test_oidc_redirect_flow::error-swallowed  # BACKLOG(#431): `except ...: pass`/return/log inside a test body — the assertions in the `try` silently stop running and nobody is told. Needs the exception to propagate or be asserted on. UNTRIAGED.
e2e/test_ldap_oidc.py::test_oidc_admin_login_full_flow::error-swallowed  # BACKLOG(#431): `except ...: pass`/return/log inside a test body — the assertions in the `try` silently stop running and nobody is told. Needs the exception to propagate or be asserted on. UNTRIAGED.
e2e/test_ldap_oidc.py::test_keycloak_regular_user_login::error-swallowed  # BACKLOG(#431): `except ...: pass`/return/log inside a test body — the assertions in the `try` silently stop running and nobody is told. Needs the exception to propagate or be asserted on. UNTRIAGED.
integration/test_redaction_pipeline.py::test_detect_and_mask::error-swallowed  # BACKLOG(#431): `except ...: pass`/return/log inside a test body — the assertions in the `try` silently stop running and nobody is told. Needs the exception to propagate or be asserted on. UNTRIAGED.
test_embedding_migration_integration.py::test_throughput_benchmark::error-swallowed  # BACKLOG(#431): `except ...: pass`/return/log inside a test body — the assertions in the `try` silently stop running and nobody is told. Needs the exception to propagate or be asserted on. UNTRIAGED.
test_external_llm.py::test_local_vllm_default_config::error-swallowed  # BACKLOG(#431): `except ...: pass`/return/log inside a test body — the assertions in the `try` silently stop running and nobody is told. Needs the exception to propagate or be asserted on. UNTRIAGED.
test_external_llm.py::test_vllm_with_api_key::error-swallowed  # BACKLOG(#431): `except ...: pass`/return/log inside a test body — the assertions in the `try` silently stop running and nobody is told. Needs the exception to propagate or be asserted on. UNTRIAGED.
test_external_llm.py::test_empty_api_key_when_required::error-swallowed  # BACKLOG(#431): `except ...: pass`/return/log inside a test body — the assertions in the `try` silently stop running and nobody is told. Needs the exception to propagate or be asserted on. UNTRIAGED.
test_external_llm.py::test_user_has_ollama_running_locally::error-swallowed  # BACKLOG(#431): `except ...: pass`/return/log inside a test body — the assertions in the `try` silently stop running and nobody is told. Needs the exception to propagate or be asserted on. UNTRIAGED.
test_external_llm.py::test_user_has_external_vllm_server::error-swallowed  # BACKLOG(#431): `except ...: pass`/return/log inside a test body — the assertions in the `try` silently stop running and nobody is told. Needs the exception to propagate or be asserted on. UNTRIAGED.
test_external_llm.py::test_openai_with_custom_base_url_for_vllm::error-swallowed  # BACKLOG(#431): `except ...: pass`/return/log inside a test body — the assertions in the `try` silently stop running and nobody is told. Needs the exception to propagate or be asserted on. UNTRIAGED.
unit/test_multipart_upload.py::test_multipart_round_trip_against_real_storage::error-swallowed  # BACKLOG(#431): `except ...: pass`/return/log inside a test body — the assertions in the `try` silently stop running and nobody is told. Needs the exception to propagate or be asserted on. UNTRIAGED.
