<!--
  Managed-edition seam stub: replaced wholesale by the commercial overlay at
  image-build time (see ../README.md). Community builds render nothing here —
  every usage is additionally gated by `isCloudEdition`, so this component is
  dead-code-eliminated from community bundles.
-->
