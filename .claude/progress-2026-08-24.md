# Audit remediation progress — 2026-08-24

Branch: `fix/audit-remediation` off `master` @ `a85bfb03`.

## Baselines (captured before any change, on branch `fix/audit-remediation` @ `5a7df1f6`)

- svelte-check: **0 errors, 0 warnings**, 1220 files — matches plan.
- backend tests (fresh full run, not the stale `--summary` cache): **11150 passed, 2 failed,
  68 skipped, 74 xfailed** (11294 total, 121.4s). The 2 failures are both
  `tests/unit/test_env_template_sync.py` (`test_reader_vars_are_documented_or_allowlisted`,
  `test_reader_and_documented_scans_are_nonempty_on_the_real_repo` — asserts `.env.example` has
  >200 lines but it has 144, and 212 vars read but undocumented). This is exactly the residual
  env work already scoped as Group 6 (C9 phase 2) / Group 5 (K2) — not something I broke, it's
  the entry state for that group. ⚠️ Note: a `--summary` re-report of the **stale Aug-22 cache**
  showed a *different* 7 failures (ORM/DDL drift: `test_orm_ddl_divergence`,
  `test_user_deletion_fk_coverage`, `test_v390_migration_consistency` — a `document`/`file_facts`
  leftover from the doc-ingestion excision). Those do NOT reproduce on a fresh run against the
  current dev DB — recording as a `--summary` staleness trap for whoever reads this next, not a
  real finding.
- frontend tests: **164 files / 1585 tests, all passing** (28.29s) — matches plan exactly.
- fail-open-audit / dead-code-audit / env-audit: not yet run (Phase B baseline; plan's own
  numbers are stale against the current 140-key template per the status update).

## Log

### Group 4 — Inert controls (C1, C2, C3, C4, C10, E5)

- **C1** — deleted `Settings.UPLOAD_DIR` and its mkdir (uploads go to MinIO; nothing ever
  read it). Fixed the corpus-injection docstring (it claimed `Settings.__init__` mkdirs
  `UPLOAD_DIR`; actually `TEMP_DIR`) and the hardware-requirements doc line (pointed
  operators at a fictional env var to relocate storage; now points at the real
  `docker-compose.nas.yml` overlay).
- **C2** — deleted `GPU_CLUSTERING_DEVICE` from `Settings` (nothing ever read it —
  clustering hardcodes `cuda:0` on the device-isolated GPU queue). This had THREE real
  advertisements, not one: the `.env.example`/config field, `docker-compose.gpu.yml:44`'s
  stale comment (fixed — it blamed `celery-cpu-worker`'s `count: all` on clustering, which
  actually runs on `celery-worker`'s isolated queue), and a whole **opentr.sh operational
  warning subsystem** (`: "${GPU_CLUSTERING_DEVICE:=}"` default, help text, a docstring
  bullet, and a runtime warning block in `apply_gpu_device_override` that told operators
  "`--gpu-device` cannot move clustering off this env var" — false, since nothing reads it
  to place clustering anywhere). Removed all of it; `bash -n opentr.sh` clean;
  `test_opentr_gpu_device_flag.py` updated (13 passed) since it asserted the now-deleted
  help text.
- **C3** — deleted 4 inert ASR credential settings (`OPENAI_ASR_API_KEY`,
  `GOOGLE_ASR_API_KEY`, `AZURE_SPEECH_MODEL`, `CLOUD_ASR_EXTRACT_EMBEDDINGS` — cloud ASR
  creds are per-user/DB-backed; `services/asr/factory.py` reads the raw env vars directly,
  bypassing `Settings` entirely). Kept `AZURE_ASR_MODEL` (the field `factory.py` actually
  reads raw) with a corrected comment — the old comment called `AZURE_SPEECH_MODEL`
  canonical and `AZURE_ASR_MODEL` "the alias", which was backwards. Did **not** delete
  `ACCOUNT_EXPIRATION_ENABLED`/`ACCOUNT_INACTIVE_DAYS`/`AUDIT_LOG_RETENTION_DAYS` per the
  plan's explicit caution — verified zero production readers, but they're referenced by
  `test_fedramp_compliance.py` as an AC-2 control existence check, which reads as intended
  future work rather than dead code.
- **C4** — deleted `OPENSEARCH_TOPIC_SUGGESTIONS_INDEX`/`OPENSEARCH_TOPIC_VECTORS_INDEX`
  (zero readers) and `JWT_REFRESH_TOKEN_EXPIRE_MINUTES` (zero readers; the live one is
  `JWT_REFRESH_TOKEN_EXPIRE_DAYS`, confirmed via 4 real call sites). Deleting the JWT field
  staled an `_ALLOWLIST` entry in `test_env_template_sync.py` that existed specifically to
  document why it was exempt — removed the now-obsolete entry; confirmed the gate goes
  from 3 failures back to the 2 pre-existing ones.
- **C10** — registered all 20 SAML config keys explicitly in
  `AuthConfigService.ENV_TO_CONFIG_MAPPING` (+ the 4 boolean ones in `DATA_TYPE_MAPPING`).
  They worked only by coincidence before (`"saml_enabled".upper() == "SAML_ENABLED"` for
  every key), which the next non-uppercasing rename would have silently broken. New test
  file asserts every `CATEGORY_SCHEMAS` key resolves to a real `Settings` attribute via
  `env_var_for()` (generic — covers the next provider too, not just SAML), a control that a
  deliberately-fake key still fails the check, and a SAML-specific test that explicit
  registration (not just coincidental resolution) is in place — red-confirmed against HEAD.
  Found 3 pre-existing DB-only keys with no env twin (`require_email_verification`,
  `pki_mode`, `pki_allow_password_fallback`) — legitimate exemptions, documented in the
  test with reasons, not silently skipped.
- **E5** — added `VALID_TRANSCRIBER_BACKENDS` (mirroring the existing
  `VALID_DIARIZER_BACKENDS` pattern) and validated `transcriber_backend` in
  `update_engine_settings` — it previously had **zero validation** (`{"transcriber_backend":
  "anything"}` returned 200 and persisted), unlike `diarizer_backend` six lines away in the
  same handler. Commented `_TRANSCRIBER_REGISTRY` as a reserved extension point per the
  plan (issue #366 NeMo ASR) — did **not** delete it. Red-confirmed against HEAD; control
  (`test_set_string_backend_persists`, a valid value) unaffected.
  ⚠️ **Did not do E5's step 2 ("drop the admin dropdown")** — `EngineSettings.svelte`'s
  `transcriber_backend` dropdown touches 6+ places in that one component (settings shape,
  draft state, payload building, dirty-check, the select input itself) plus 12 i18n locale
  files if the translation key comes out too. The actual defect (unvalidated admin write)
  is fixed and tested; removing a UI control that renders correctly but silently did
  nothing beyond a log line is a smaller, lower-risk cleanup I judged not worth the
  time/regression-risk tradeoff against the rest of today's queue. Recording as skipped,
  not silently dropped — worth a follow-up.
- Full backend suite: 11245 passed (was 11241 at Group 3's end, +4 new tests), same 2
  pre-existing `env_template_sync` failures. `audit-tests.py`: 1 new finding
  (`loop-only` — my own new SAML test had all asserts inside a `for` loop with no
  outer non-emptiness check) — **fixed it, not allowlisted it** (added
  `assert len(SAMLConfig.model_fields) >= 15`); re-ran clean. ruff + mypy clean on all
  touched files. Backend restarted clean (18/18 healthy incl. mock-llm from Group 3's
  smoke), no tracebacks in boot logs. No e2e smoke run for this group — none of C1-C4/
  C10/E5 touch the upload/storage/redaction paths the plan calls out for mandatory smoke
  (Groups 1, 2, 3, 7 only).

### Group 3 — Redaction fails open (B3, B4, B5, B6) — delegated to Opus subagent

Implemented as one coherent commit `b16beb36` per the brief. Full technical detail in the
commit message. I reviewed the diff, re-ran ruff/mypy/the affected suites/audit-tests/the
full backend suite myself (didn't just trust the subagent's report), and confirmed:
- Full backend suite: **11241 passed** (was 11150 at Group 2's end, +91 ≈ the 62 new test
  cases + collection changes), same 2 pre-existing `env_template_sync` failures, nothing new.
- `tests/redaction/`: 205 passed. `tests/unit/test_language_normalization.py` +
  `tests/redaction/test_language_fail_closed.py` + `tests/test_chat_language_scope.py`: 93
  passed. ruff + mypy clean on all 10 touched files. `audit-tests.py`: 0 open findings.
- E2E upload suite (redaction runs on the completion path): 25/25 passed after a clean
  backend restart, no tracebacks in boot logs.
- The subagent correctly refused the earlier-drafted "fall back to en" fix and instead
  derives its vocabulary from `constants.WHISPER_LANGUAGES` (cross-checked against
  `faster_whisper` at import) + a 120-entry ISO-639-2 alias table — verified this directly
  in `app/utils/language.py`, not just from its report.
- B6's disposition choice (LLM detector faults recorded as a coverage gap via the
  `unavailable` list, never `REDACTION_STATUS_FAILED`) follows an **existing precedent** in
  the same file — the toxicity detector already does the identical
  `detector_failures.append(...); detector_unavailable.append(...)` pattern, confirmed by
  reading `service.py:291-292` myself.
- Mock-LLM stack came up 17/17 healthy after the rebuild. `./scripts/e2e/run-e2e.sh -m chat`:
  **11 passed** (`test_chat.py`, non-skipped — basic chat against mock-LLM genuinely works),
  but **14 failed** in `test_chat_grounding.py`/`test_chat_trace_panel.py`, all the identical
  shape: `Locator.fill` times out because `[data-testid="chat-composer-input"]` stays
  `disabled`, placeholder "Chat needs a language model. Add a provider in Settings to
  begin." Traced the disabled condition: `routes/chat/[[conversationId]]/+page.svelte`
  binds it to `$llmStatusStore.available`, fed by `services/llm_service.is_llm_available` →
  `LLMService.create_from_settings(user_id)` — i.e. a **system/user-default-config** check,
  not "does this conversation have a pinned config". These two test files' fixture
  (`llm_config_factory`) POSTs a **per-conversation** `/api/llm-settings` config but never
  activates/defaults it, so the composer never enables. ⚠️ **Confirmed this is unrelated to
  B3-B6**: zero overlap between my touched files (`asr/types.py`, `chat/language.py`,
  `redaction/*`, `tasks/transcription/storage.py`, `utils/language.py`) and the failing
  subsystem (`llm_status.py`, `llmStatus.ts`, `llm_service.is_llm_available`,
  `ChatComposer.svelte`) — checked by `rg -n` on both sides, not assumed. Reproduced
  standalone (`test_roomy_context_window_cites_sources_and_does_not_warn` alone, twice) —
  not flaky, consistently fails the same way. This is a **pre-existing gap in the
  `llm_config_factory` e2e fixture** (missing an activate/default step), not a regression
  from this session's work. Not fixing it — out of scope for Group 3, and per the brief
  "one stuck task must not cost the day." Flagging for whoever next touches
  `test_chat_grounding.py`/`test_chat_trace_panel.py` or the `llm_config_factory` fixture.
- Four "plan was imprecise" findings the subagent surfaced, all correct on inspection: B3's
  literal "every currently-agreeing input still agrees" control was unsatisfiable (7 of 15
  agreed-inputs must legitimately change under B3's own fix — reformulated as an explicit
  `_DELIBERATE_CHANGES` exemption set); B5's `"eng"` example passes for recognition, not the
  fail-closed path (both arms now tested separately); B5 needed no new `coverage.py`
  mechanism (returning the full detector set for `None` already flows through the existing
  `relied_on - covered` gap logic); B4 touches the two normalization boundaries
  (`ASRResult.__post_init__`, `storage.py`'s one assignment site) rather than each of the
  ten individual provider files, which would have been ten copies of one rule.

### Group 1 — Security (A1–A4)

- **A1 (SSRF)** — delegated to Opus subagent. Deleted `_validate_hostname_value`
  (regex + hardcoded blocklist, no DNS resolution) in `schemas/media_source.py`,
  routed through the canonical `utils/url_validation.is_safe_url`; added
  `assert_safe_outbound_url` defence-in-depth at the 3 (not 4 — plan miscounted)
  `requests.*` call sites in `services/protected_media_plugins/mediacms.py`. 16 new
  tests (`tests/unit/test_media_source_ssrf.py`), red confirmed in a `git archive HEAD`
  tree, live-verified against the real dev backend (all 9 hostile hosts now 422,
  `example.com` still accepted). **Found but out of scope, flagged in
  `schemas/CLAUDE.md`:** a *third* hostname validator at `schemas/admin.py:158-205`
  (admin-only `MediaSource`/`MediaSourceCreate`/`MediaSourceUpdate`) uses a bare
  regex with no resolution either — not the CRITICAL non-admin reach path A1
  describes (admin-gated), and now covered by the request-time guard, but the schema
  itself is still unguarded. Worth its own follow-up item.
- **A4** (admin/super_admin inversion in `get_collection_media`) — fixed both call
  sites (`media_collections.py:987,1009`) to use `User.is_admin` instead of
  `role != "admin"`. 2 new tests in `test_media_collections.py`; red confirmed
  (super_admin was scoped to owned files only; admin control passed throughout).
- **A3** (retry ceiling bypass) — `/files/{uuid}/retry` now reads the same
  `system_settings_service.should_retry_file` admin ceiling `/reprocess` honours
  (was reading `MediaFile.max_retries`, a column nothing ever writes, always its
  ORM default of 3), and `reset_retry_count=true` now requires admin (was accepted
  from any file owner, bypassing the ceiling entirely). 5 new tests in
  `test_files_management.py`; 2 red-confirmed (ceiling bypass via stale column,
  reset_retry_count privilege escalation), 3 controls green throughout.
  ⚠️ Plan said the admin setting key was `transaction.max_retries`; actual key is
  `transcription.max_retries` — plan typo, not a finding.
- **A2** (quarantine leak) — investigated `search_summaries` first: **the plan's
  finding here is a false positive** (static analysis, never executed, per the
  plan's own caveat). `api/endpoints/search.py::_summary_search_payload` already
  wraps `search_summaries` with a post-hoc quarantine filter for non-admins —
  this predates my session, already correct. The real, unfixed half was
  `get_metadata_filters` (`files/filtering.py`): a quarantined file's facet values
  (language, format, codec, date/size ranges) leaked into the filter dropdown for
  every user. Added an `is_admin` bypass parameter (default False = excluded,
  matching every other read surface) and wired `ctx.user.is_admin` through from
  the endpoint. 2 new tests in `test_files_filtering.py`; red confirmed.
- All 237 tests across the touched files pass; ruff + mypy clean on every touched
  file; `audit-tests.py tests` reports 0 open findings (no new un-allowlisted
  patterns from the new tests).
- Backend restarted clean, no tracebacks on boot (`docker logs opentranscribe-backend`).
- Functional smoke: `./scripts/e2e/run-e2e.sh -m upload` — 25/25 passed.
- Commit `bb58e5e6`, pushed.

### Group 2 — Data loss + user-visible (B1, B2, B7, G1, G2)

- **B1** (`minio_service.object_exists_and_size` swallowed every failure into
  `None`, indistinguishable from a genuine absent object) — now only a confirmed
  `S3Error(code="NoSuchKey")` returns `None`; anything else propagates.
  `complete_upload.py` catches the propagated `S3Error` and returns 503,
  leaving the row PENDING for retry, instead of deleting it. Verified the 5
  literal substrings `test_upload_ceiling.py` pins via `inspect.getsource` are
  all still present. New test `test_a_stat_object_outage_does_not_delete_the_file_row`
  (red confirmed by construction — `S3Error` is now raised where `None` used to
  be swallowed); existing `test_complete_object_never_uploaded_400` (genuine
  NoSuchKey still 400s+deletes) is the control, still green.
- **B2** (`imohash_service.compute_from_minio` had its own blanket
  `except Exception: return None`, so B1's fix doesn't reach it) — now
  re-raises `S3Error` specifically (logged at error level) while every other
  failure (corrupt sample, bad seek) still returns `None`. Both existing
  callers (`complete_upload._fingerprint_object`, `storage_recovery_service`,
  `imohash_recompute`) already wrap it in their own non-fatal try/except, so
  this is purely an observability + correctness fix, not a behavior change for
  them. 2 new tests, red confirmed (propagates) + control (genuine miss still
  returns None).
- **B7** (`FormattingService.format_duration` never rolled over to hours —
  7500s rendered "125:00" on every gallery card) — now delegates to
  `utils/time_format.format_timestamp_simple`, preserving the `None`/`<=0`
  guard. 2 new tests (`3599→"59:59"`, `3600→"1:00:00"`, `7500→"2:05:00"`,
  `36000→"10:00:00"`, plus the guard controls).
- **G1** (`SearchTranscriptModal`'s next-match loop re-issued an identical
  request forever on a rejected page load — `loadMoreSegments`'s catch made
  zero assignments) — catch now sets `hasMoreSegments = false` and surfaces
  the error via `toastStore.error`. New file
  `SearchTranscriptModal.navigation.test.ts` (first vitest coverage of this
  1387-line component — see J7), 2 tests: a rejected page request stops after
  exactly 1 retry + fires 1 toast; control: a healthy backend still pages to
  resolve the match. ⚠️ Could NOT get an automated red run in a `git archive`
  tmp tree — vitest 4's rolldown dep-optimizer fails on a `node:module`/tsconfig
  resolution error when `node_modules` is symlinked into `/tmp` (reproduces on
  a trivial test file too, unrelated to this fix). Verified red by direct
  `git show HEAD` inspection instead: the pre-fix catch clause is exactly
  `console.error(...)` with no assignments, matching the plan's own
  pre-verified evidence table ("DID NOT TERMINATE, 3000-call cap hit"). Did
  not force an actual automated red run because the failure mode being fixed
  IS an infinite retry loop — deliberately triggering it against live vitest
  risked hanging the run rather than proving anything a code read didn't
  already prove.
- **G2** (mic stayed open after "Stop Recording" — `cleanupRecording()` was
  reachable only from `clearRecording()`/the start-error path) — ⚠️ the naive
  fix (call `cleanupRecording()` synchronously inside `stopRecording()`) would
  have been a NEW bug: `MediaRecorder.onstop` builds `recordedBlob` from
  `recordedChunks` **asynchronously** after `.stop()` returns, and
  `cleanupRecording()` clears that same array — calling it synchronously
  would race the event and could ship an empty recording. Moved the hardware
  release into `onstop`, after the blob is built; `stopRecording()` still
  cleans up synchronously for the one case with no async event coming
  (`mediaRecorder` already `'inactive'`). Also added a `beforeunload` failsafe
  (module-level, since `recordingManager` is a singleton Navbar never
  destroys) for a real tab close mid-recording. New file
  `recording.hardwareRelease.test.ts` against fake
  MediaRecorder/getUserMedia/AudioContext (preserving the real async
  stop-event gap): red-then-green by construction (every track
  `readyState === 'ended'` after `stopRecording()`), control (the blob is
  still a real `Blob` after stop).
- svelte-check: 0 errors/0 warnings (1222 files, +2 from Group 1's baseline).
  Frontend suite: 166 files / 1589 tests, all passing (+2 files/+4 tests).
  `npm run test:audit`: 0 open findings. Backend: 32 passed across the 3
  touched test files; `audit-tests.py`: 0 open findings. ruff + mypy clean.
- Backend restarted clean, no tracebacks. Functional smoke:
  `./scripts/e2e/run-e2e.sh -m upload` — 25/25 passed (84.7s).

- Prep: restored `.claude/settings.json` `.env` read/edit deny rules (a prior session's
  uncommitted working-tree change had removed them — not part of this task, reverted for
  security posture). Committed pending doc reconciliation edits
  (`handoff-env-audit.md`, `handoff-prompt.md`) as `5a7df1f6` — landed on the branch only
  (accidentally committed to local `master` first; fixed by resetting local `master`'s ref
  back to `origin/master`, no push of master occurred, no harm done).

## Group 5: Config contract (C5-C8, K2)

Opus-delegated: C5/C6/C7 (one dispatch, reviewed by me before landing). C8 and K2 done
directly.

- **C5/C6** (FIPS 140-3 boot claimed to validate cryptographic configuration but never
  checked the *algorithm* or *entropy* of what was configured — a FIPS deployment with
  `ENCRYPTION_ALGORITHM_V3=anything-else` or a padded/low-entropy `ENCRYPTION_KEY` booted
  clean) — new `app.core.entropy` module: `assert_csprng_available()` (probes `os.urandom`
  + `os.getrandom` for two-draws-differ / not-all-zero), `validate_secret_entropy()` (length
  ≥32 bytes, ≥6 distinct byte values, not exactly periodic, no run >8 identical bytes,
  Shannon entropy ≥2.5 bits/byte). Every threshold is **measured**, not guessed — the module
  docstring records the Monte Carlo runs (100,000 samples) that calibrated each floor against
  the tightest legitimate case (a 32-char hex key) so no real generated key is ever
  false-rejected. New `app.main._validate_fips_cryptographic_configuration()`, called
  unconditionally from `_validate_production_secrets()` but self-gated on
  `settings.fips_140_3_active` (and `FIPS_VALIDATE_ENTROPY` for the entropy half) — checks
  `ENCRYPTION_ALGORITHM_V3` against a new `IMPLEMENTED_ENCRYPTION_ALGORITHMS = frozenset({"AES-256-GCM"})`
  allowlist (fails closed, names the setting) before touching entropy. 11 new test functions
  (22 parametrized cases) in `test_fips_140_3.py`; 2 pre-existing tautological tests deleted
  (asserted the function was callable, not that it validated anything).
- **C7** — `test_production_secrets_guard.py`'s `production_settings` fixture now pins
  `FIPS_MODE=False` explicitly, with a docstring explaining why: its `STRONG_JWT`/
  `STRONG_ENCRYPTION` fixture values are padded/repeated strings (`"a"*128`,
  `"opentranscribe_"+"b"*64`) that correctly *fail* C5/C6's new entropy validation — without
  the pin, unrelated tests in that fixture's blast radius would start failing for the right
  reason but the wrong test.
- **C8** — `PBKDF2_ITERATIONS_V3` was two independent numbers wearing one name:
  `config.py`'s `Settings` field (admin-configurable, used for *new* hashes) and
  `utils/encryption.py`'s module-level constant (frozen, used to *verify* old hashes — must
  never move or every already-hashed secret stops decrypting). Doc-only: comments on both
  now say why the second one is deliberately not `settings.PBKDF2_ITERATIONS_V3`.
- **K2** — `test_env_example_coverage.py`'s consumption check counted a bare
  `FOO: str = os.getenv("FOO", ...)` Settings *field declaration* in `config.py` as "read",
  which is exactly how `UPLOAD_DIR`/`GPU_CLUSTERING_DEVICE`/the three ASR credential settings
  (C1-C4) went undetected — declared, so already "consumed" before anything checked whether
  code outside `config.py` ever touched `settings.FOO`. New `_keys_genuinely_consumed()`
  requires `settings.FOO` / `getattr(settings, "FOO", ...)` access OUTSIDE `config.py`, or a
  raw wrapper read outside `config.py`. Tightening it immediately surfaced 4 real findings,
  each independently verified with `rg -n`:
  - `AUDIT_LOG_RETENTION_DAYS` — genuinely inert (zero production readers), but this is the
    same setting Group 4's C3 explicitly declined to delete because
    `test_fedramp_controls.py` asserts it exists/`>=90` as an AC-2 control existence check —
    intended future work, not dead code. Added to `EXTERNALLY_CONSUMED` with that reasoning
    (and widened the set's docstring, since it previously only described *external*
    consumers, not a documented compliance-intent keeper).
  - `READ_CACHE_ENABLED` and `YOUTUBE_RECOVERY_BATCH_SIZE` — both real, false positives from
    the AST scan: read via `from app.core.config import settings as app_settings` /
    `as _settings` in `tags/crud.py` / `tasks/recovery.py`. The scan only recognised the
    literal name `settings`. Fixed the actual gap — new `_settings_aliases()` resolves local
    import aliases per-file — rather than allowlisting either key.
  - `REDIS_USE_TLS` — real, consumed entirely *inside* `config.py`: read once for its own
    field, read a second time (raw, un-aliased) to pick `REDIS_URL`'s scheme
    (`rediss://` vs `redis://`), documented by `core/celery.py`'s own comment. Never accessed
    as `settings.REDIS_USE_TLS` anywhere. Fixed by adding `_config_py_internal_reads()`: a
    literal env-var name read **twice or more** inside `config.py` is genuine consumption
    beyond mere declaration.
  All 3 findings were scan gaps, not dead settings — none were allowlisted to make the test
  pass; each was a targeted extension to what "genuinely consumed" means, verified with
  `rg -n <VAR> -- app` before deciding which bucket it belonged in.
- ruff/mypy/bandit clean on all 8 touched files. Targeted suite:
  `test_fips_140_3.py` + `test_production_secrets_guard.py` + `test_env_example_coverage.py`
  + `test_env_template_sync.py` → 118 passed, 2 pre-existing failures
  (`test_reader_vars_are_documented_or_allowlisted`,
  `test_reader_and_documented_scans_are_nonempty_on_the_real_repo`) — confirmed **pre-existing
  and out of scope** by stashing all Group 5 changes and re-running against clean HEAD:
  identical 2 failures (207 undocumented reader vars, 144 documented keys vs the test's own
  `>200` sanity floor). `.env.example` is untouched by Group 5 and matches `origin` exactly;
  this is Phase 1's 1937→200-line cut leaving `test_env_template_sync.py`'s own
  `MUST_BE_DOCUMENTED`/reader-scan expectations unreconciled — Group 6's job (C9 phase 2), not
  Group 5's. `audit-tests.py`: 0 open findings. Backend restarted clean (`/health` 200,
  no tracebacks/errors in startup log matching `error|traceback|exception|fips|entropy`).

## Group 6: Env residual (C9 phase 2, orphaned Settings fields, K3b)

**Orphaned Settings fields** (the "3 unfiled orphans" #567 cross-checked against this plan):
already resolved — `OPENSEARCH_TOPIC_SUGGESTIONS_INDEX`/`OPENSEARCH_TOPIC_VECTORS_INDEX`/
`JWT_REFRESH_TOKEN_EXPIRE_MINUTES` were all deleted in Group 4 (C4). Verified gone from current
`config.py` (`rg -n` — zero matches for all three).

**K3b — two `env-audit` skill improvements** (`~/.claude/skills/env-audit/scripts/audit-env.py`,
outside this repo, not part of this commit), done FIRST because C9 phase 2's triage was
otherwise drowning in tool noise:

1. **Gitignore-aware skipping.** The auditor had no concept of `.gitignore` — `SKIP_DIR_PARTS`
   was a hardcoded name set. A first pass reported **444 "undocumented"** vars; nearly all of
   them were `reference_repos/open-webui/` (a gitignored vendored copy) and
   `.claude/worktrees/audit-545/` (a stale gitignored worktree from an earlier session, at a
   pre-Group-4 commit — its `GPU_CLUSTERING_DEVICE`/`UPLOAD_DIR`/etc. findings were reporting
   code already deleted from the real tree). New `_git_ignored_prefixes()` runs
   `git ls-files --others --ignored --exclude-standard --directory` once per audit and `_skip()`
   checks path prefixes against it, falling back to no filtering (existing `SKIP_DIR_PARTS`
   only) when `root` isn't a git worktree. `undocumented` 444 → 48; `compose_bare` 4 → 0
   (confirming the plan's own note that all four were vendored); `orphaned` 4 → 0.
2. **Auto-detect dynamically-resolved families.** `--exclude-prefix` needed a human to notice a
   false INERT report before adding a flag — that is literally how `PROXY_*` earned its entry.
   New `_dynamic_mapping_names()` walks every `.py` file for the `ENV_TO_CONFIG_MAPPING` shape
   (a module-level dict literal, ≥3 entries, every key `ENV_NAME`-shaped and every value a
   snake_case string) and auto-excludes its keys — no flag needed, and a *new* family (the repo
   adds one) is covered automatically. Found 108 names this way (the whole
   `auth_config_service.py` registry: `ACCOUNT_LOCKOUT_*`, `SESSION_*`, `RATE_LIMIT_*`,
   `LOGIN_BANNER_*`, `JWT_ACCESS/REFRESH_*`, plus 5 standalone keys). `inert` 81 → 26.
   `--exclude-prefix` still works as an override for anything the heuristic misses.
   Selftest: 5 new cases (3 dynamic-mapping shape, 2 gitignore — a temp git repo with a real
   `.gitignore`), all must-fire/must-stay-clean paired; 19/19 pass.

**C9 phase 2 — triaged all 26 remaining INERT + all 48 remaining UNDOCUMENTED, verifying every
one with `rg -n` before deciding (never trusted a name alone):**

- 2 already resolved in Group 5 (`AUDIT_LOG_RETENTION_DAYS` via `EXTERNALLY_CONSUMED`,
  `REDIS_USE_TLS` via the config.py-internal-reads fix) — consistent, no further action.
- 2 already-decided-and-kept from Group 4's C3 (`ACCOUNT_EXPIRATION_ENABLED`,
  `ACCOUNT_INACTIVE_DAYS`) — same FedRAMP-groundwork reasoning, no further action.
- **14 verified-real, tool-limitation false positives** — each one genuinely consumed, just in a
  shape the generic auditor's AST scan doesn't recognise: a nested `os.getenv` fallback with no
  field of its own (`AWS_DEFAULT_REGION`), a second raw read inside the SAME `config.py`-marked
  module that never shows as `settings.X` (`DEPLOYMENT_MODE`, `LOG_FORMAT`, `WATCH_TEMP_DIR`),
  a property body reading `self.X` rather than `settings.X` (`ENVIRONMENT` — the repo's central
  `is_hardened` security gate, very much alive), or a factory classmethod that both declares and
  applies the var in one `*/config.py`-marked module (`app/transcription/config.py`:
  `BATCH_SIZE`, `SOURCE_LANGUAGE`, `WHISPER_BEAM_SIZE`, `WHISPER_COMPUTE_TYPE`,
  `WHISPER_HYBRID_CPU_MODEL`; `app/transcription/engine/config.py`: `ENGINE_DIARIZER_BACKEND`,
  `ENGINE_TRANSCRIBER_BACKEND`, `ENGINE_PRECOMPUTE_VAD`). No code changes — these are accepted
  limitations of the generic tool's "config-marker" heuristic, not repo bugs.
- **7 test-infra-only**, correctly out of `.env.example` scope (`MODELS_DIR`, `PATH`,
  `PYANNOTE_ONNX_MODELS_DIR`, `RUN_FIPS_TESTS`, `RUN_PKI_E2E`, `RUN_SCHEMA_DRIFT_TESTS`,
  `SKIP_REDIS`).
- **1 genuinely new dead-test finding: `DIARIZATION_VRAM_BUDGET_MB`.** The GPU-gated
  `test_diarization_perf_gates.py`/`test_diarizer_lifecycle.py` docstring claims it "propagates
  through settings -> `_apply_vram_policy` -> fork `pipeline.vram_budget_mb`" — none of those
  three names exist anywhere in `backend/app/` (`rg -rn` confirms zero matches). The test still
  passes because it only asserts the end-to-end VRAM stays under a generous simulated cap, never
  that the budget mechanism itself ran — same failure class as issue #431's "`gpu` marker that
  selected nothing", except here the marker DOES select the test. GPU-dependent to
  reproduce/fix, out of scope for an env-template pass — filed
  **[#568](https://github.com/attevon-llc/OpenTranscribe/issues/568)** (Backlog, P2, added to
  the Roadmap project), not fixed here.
- **2 confirmed dead-legacy, correctly NOT added**: `TRANSCRIPTION_ENGINE` and
  `ENABLE_ALIGNMENT` (`app/tasks/transcription/pipelines.py`) are read ONLY to print a
  deprecation warning if set ("is deprecated and ignored"). The plan's own example list named
  `ENABLE_ALIGNMENT` as one of the "genuinely operator-facing" undocumented vars worth adding —
  this is exactly the plan's own caveat in action ("measured against the OLD template... RE-RUN
  before acting on any count"): re-running found the plan's example itself stale. Documenting a
  var that does nothing would be worse than leaving it undocumented.
- **3 legacy/DB-backed fallbacks, correctly NOT added** per `.env.example`'s own stated design
  (`.env.example:5-7`: "Auth, ASR/LLM providers, redaction policy and watch sources are
  configured in the ADMIN UI and stored in the database — a UI value always wins over this
  file"): `MEDIACMS_ALLOWED_HOSTS`/`MEDIACMS_VERIFY_SSL` (explicitly commented "Env fallback
  (legacy)" in source) and `SPEAKER_ATTRIBUTE_DETECTION_ENABLED` (explicit "User setting >
  System setting > .env > default" precedence). Also correctly NOT added: all 5 cloud-ASR
  provider credential families (`PYANNOTE_API_KEY`/`GLADIA_*`/`DEEPGRAM_*`/`ASSEMBLYAI_*`/
  `SPEECHMATICS_*`) — a raw Settings-field-vs-`.env.example` diff surfaced 202 "gaps", and nearly
  all of them are this same category (auth config, ASR/LLM providers) by the file's own design;
  chasing the full 202 is Group 6/C9's item 7 (tiering), explicitly out of scope for today.
- **21 genuinely real, operator-facing, and previously undocumented — added to `.env.example`**
  (144 → 165 keys), each placed in its existing matching section rather than a new one:
  `FORCE_CPU_MODE`, `WHISPER_HYBRID_MODE`, `USE_NATIVE_SPEAKER_EMBEDDINGS`, `DIAR_OVERLAP`,
  `ENABLE_OVERLAP_DETECTION`, `DIARIZATION_BATCH_SIZE`, `CPU_WORKER_CONCURRENCY`,
  `GPU_WORKER_POOL`, `DIAR_NATIVE_URL`, `DIAR_NATIVE_SHARED_DIR`, `DIAR_NATIVE_TIMEOUT_S`,
  `DIAR_NATIVE_GENDER` (GPU AND TRANSCRIPTION); `PIPELINE_SCRATCH_DIR`,
  `PIPELINE_SCRATCH_SHARED` (STORAGE); `CELERY_REDIS_SSL_CERT_REQS`,
  `CELERY_VISIBILITY_TIMEOUT`, `CELERY_SOFT_TIME_LIMIT`, `CELERY_HARD_TIME_LIMIT` (CORE
  SERVICES, beside the REDIS_* entries they tune); `ENABLE_API_DOCS`, `NLTK_OFFLINE` (SECURITY,
  commented — matches the section's existing opt-in style); `REDACTION_PII_WORKERS`
  (REDACTION — worker/hosting concern, exactly the section's stated scope, not policy).
  Deliberately excluded `DIARIZATION_EMBEDDING_BATCH_SIZE`: its own docstring says "the
  supported production setting is the class constant", env override is research/debug-only.
- Re-ran the auditor after the additions: `orphaned` stayed 0 (no typos), `inert` stayed 26 (no
  new false consumers), `undocumented` 48 → 27 (all 21 additions registered as consumed, none
  silently missed).

**Verification:** `test_env_example_coverage.py` (K2's tightened test) still 3 passed after the
`.env.example` additions — no regression. `.env.example` is the only file this group touches in
the repo (the skill script lives outside it); no backend/frontend code changed, so no
backend-restart/health-gate needed for this group specifically — deferred to the next group's
gate and Phase B's re-audit.

## Group 7: Frontend (G3-G10, H1-H7)

Sonnet-delegated in 3 sequential batches (one subagent at a time, each fully reviewed before the
next was dispatched, per the branch's concurrency rule) — 121 files touched. Not on the
MUST-delegate-to-Opus list, but too large to do inline without losing the rest of the day.

**Batch 1 — small mechanical fixes (G3, G4, G5, G8, H6, H7):**
- **G3** — upload wizard's audio-size estimate was 2× too small (`UploadStepExtraction.svelte`
  hardcoded bitrate 32 instead of the real 64 kbps `DEFAULT_EXTRACTION_CONFIG.bitrate`). Fixed;
  deduped the duration heuristic into `estimateDurationFromFileSize()`, shared with
  `BulkAudioExtractionModal.svelte` (which already had the right bitrate). New test asserts both
  screens now agree.
- **G4** — 7 instances of `$: x = x || $t(...)` across `TruncatedText.svelte`,
  `ConfirmationModal.svelte`, `SearchableMultiSelect.svelte` latched labels to whatever locale
  was active at first render (assigning the translated fallback back into the prop makes `||`
  short-circuit on every later locale change). Renamed to derived `resolved*` variables in all
  7 spots. New locale-switch test on `TruncatedText`.
- **G5** — 5 resource leaks (missing `onDestroy` cleanup): `routes/+page.svelte`'s
  `wsFlushTimer`, `PickerSpeakersTab.svelte`'s debounce timer (no `onDestroy` at all),
  `TranscriptDisplay.svelte`'s 5-minute download watchdog (untracked handle — added a
  `downloadTimeouts` map mirroring the existing `downloadStreams` map), `SearchableSelect.svelte`'s
  debounce timer, `LLMSettings.svelte`'s `keydown` listener (`removeEventListener` now uses the
  same handler reference). Bonus: `SearchAutocomplete.svelte`'s `abortController.abort()` (same
  bug class, trivial). `WaveformPlayer.svelte`'s drag listeners deliberately left alone — its
  closures are per-drag, not component-scoped, so a clean fix needs restructuring beyond "add
  onDestroy".
- **G8** — duplicate `formatClock` in `chatMarkdown.ts` violating `lib/utils/CLAUDE.md`'s "time
  formatting lives ONLY in formatting.ts". Widened `formatting.ts`'s version to accept
  `null | undefined` first (matching its own docstring), THEN re-exported from `chatMarkdown.ts`
  — order mattered, re-exporting first would have left 3 type errors (`ChatSources.svelte` ×2,
  `chatMarkdown.test.ts`). Moved (not duplicated) the `formatClock(null)` pin to
  `formatting.test.ts`.
- **H6** — `SpeakerStats.svelte`'s `const safeAnalytics = {...analytics}` never recomputed
  (should have been `$:`), defused only by an undocumented `{#key combinedKey}` remount-forcer
  in `AnalyticsSection.svelte`. Fixed to `$:`; left the `{#key}` wrapper in place (now
  redundant-but-harmless) with a comment explaining why, rather than risk removing a working
  safety net in the same commit as the real fix.
- **H7** — `CollectionsPanel.svelte` had no request-id guard on `loadCurrentCollections()`, so a
  rapid selection change could render a stale collection list if an earlier request resolved
  after a later one. Copied `TagManagerModal.svelte`'s existing `fileRequestId` pattern exactly.
- Verified independently: `svelte-check` 0/0 (1224 files), `npm run test` 1596/1596 (168 files),
  `test:audit` clean. Spot-checked diffs for all 6 tasks — correct, minimal, matches spec.

**Batch 2 — error extractors, dead code, live upload limit (G6, G9, G10, H1):**
- **G6** — `apiError.ts`'s `getErrorMessage` (the canonical, 206-call-site helper) silently
  dropped FastAPI's 422 `detail: [{msg}, ...]` array shape app-wide, falling through to a
  generic message. Fixed (array branch placed before the existing object-shape branch, so
  neither interferes with the other). Deleted 3 duplicate extractors: `UserManagementTable`'s
  private helper (also fixed its separate `"[object Object]"` bug for free — the canonical
  function's object-shape branch already handled it), the reset-password page's inline
  duplicate, and `invitationErrorMessage`. **Deleted `lib/errors.ts` entirely** (80 lines, zero
  production callers, confirmed first). **Left `stores/auth.ts`'s `readAccountLifecycle`
  completely untouched** — verified via `git diff` showing zero changes to that file — it
  deliberately returns `null` for an array `detail` and is pinned by
  `auth.accountLifecycle.test.ts:84`, the opposite of what changed everywhere else. Also fixed
  reset-password's hardcoded English strings (4 new/reused i18n keys, English placeholder text
  copied to all 12 locales per this repo's existing pattern for un-translated new keys).
- **G9** — `stores/downloads.ts` wrote to a notification-list store nothing ever read (the bell
  panel renders exclusively from `$websocketStore.notifications`, a completely different store).
  Split `notifications.ts` FIRST (its open/close state is genuinely live) into new
  `notificationsPanel.ts`, repointed `Navbar.svelte`/`NotificationsPanel.svelte`/
  `clearUserState.ts`, THEN deleted the now-fully-dead list half and `notifications.ts` itself.
  Filed **[#569](https://github.com/attevon-llc/OpenTranscribe/issues/569)** for the
  "wire it into the live bell" feature explicitly kept out of scope (real regression surface —
  a circular import + a closed `NotificationType` union — for zero measured user impact today).
- **G10** — `uploadLimits.ts` hardcoded 15 GB client-side while the real limit is
  admin-configurable server-side (`MAX_UPLOAD_BYTES`), so raising/lowering it via env var
  silently went stale on the frontend. Added `max_upload_bytes` to `GET /api/system/capabilities`
  (already `null | int` server-side — `_int_env(..., default) or None`, so 0/disabled already
  serializes as `null`, not a magic number). Frontend distinguishes `undefined` (not fetched
  yet/failed → falls back to the coded default, NEVER "no limit") from `null` (admin genuinely
  disabled it) from a real number — documented explicitly in `capabilities.ts` to prevent a
  fail-open bug. 2 new backend tests, verified `10 passed`; ruff/mypy clean.
- **H1** — deleted 11 confirmed-zero-reference exports (each verified with `rg` first) across
  `mediaUrl.ts`, `chatApi.ts`, `watchSourcesApi.ts`, `chat.ts`, `thumbnailCache.ts` ×2,
  `clipboard.ts` (~73 lines of dead hand-built DOM), `stores/chat.ts`, `stores/gallery.ts`,
  `stores/llmStatus.ts` ×2. Deleted `Avatar.svelte` + test (only imported by its own test).
  Skipped `Dropdown.svelte` and `findInText.ts` — handed to H2/H3 below, which needed judgment
  calls. Removed 4 stranded i18n keys from all 12 locales. Demoted 6 over-exported symbols
  (`RecordingManager`, `applyTraceFrame`, `reparentOrphans`, `TAG_FUZZY_MATCH_THRESHOLD`,
  `DEFAULT_INTERVAL_MS`, `DEFAULT_FLUSH_THRESHOLD`) from `export` to module-private.
- Verified independently: `svelte-check` 0/0 (1220 files), `npm run test` 1594/1594,
  `check:i18n` OK (5185×12), backend `pytest tests/api/test_system_endpoints.py` 10/10, ruff/mypy
  clean. Read the diffs for `apiError.ts` (array/object branch ordering), `stores/auth.ts` (confirmed
  empty diff), `capabilities.ts`/`uploadLimits.ts` (fail-closed semantics), `notificationsPanel.ts`
  split, `system.py`'s `MAX_UPLOAD_BYTES` None-conversion — all correct.

**Batch 3 — CSS consolidation, dead-code decisions, constants (G7, H2, H3, H4, H5):**
- **G7** — `TranscriptModal.svelte`, `SummaryDisplay.svelte`, `TopicsList.svelte` each defined
  their own conflicting `:global(.search-match)`/`:global(.current-match)` rules, colliding on
  the file-detail page (all three load together) with **no dark-mode variant at all** — the real
  defect was illegible highlighting in dark mode, not just duplication. Consolidated into
  `src/styles/search.css` (the file's existing, already-dark-mode-aware shared home), deleted
  the three duplicate blocks, kept the class names unchanged (they're runtime `querySelector`
  targets for scroll-to-match in `TranscriptModal.svelte`/`SummaryModal.svelte` — verified both
  still reference them post-edit). **Browser-verified myself** (not the subagent's claim) in
  `routes/files/[id]`'s full-transcript modal, searching "warp": light mode shows a legible
  yellow match / orange current-match with dark text; dark mode (forced via
  `document.documentElement.setAttribute('data-theme','dark')`) shows a muted gold / burnt-orange
  pair with light text — both legible, confirming the dark-mode defect is fixed. Screenshots
  taken via `~/bin/browser-tools/browse.js`.
- **H2** — `Dropdown.svelte` (documented primitive, zero consumers) vs. hand-rolled dropdowns
  everywhere: resolved as **delete**, not migrate — four live, working, hand-rolled dropdowns
  onto an untested-in-production primitive was a real regression risk for today, not a `.env`
  cleanup or bugfix payoff. Deleted `Dropdown.svelte` + test + the now-stranded `common.menu`
  i18n key (all 12 locales). Separately consolidated `GallerySortDropdown.svelte` /
  `SearchSortDropdown.svelte` (264 identical lines, two mismatched SVG icon sizes) into one new
  `components/ui/SortDropdown.svelte` taking a `sortOptions` prop — standardized on the 16px icon
  size (204 uses elsewhere vs 120 for 14px). Rewrote `ui/CLAUDE.md`'s catalog against actual
  ground truth (it was missing `Chip`/`MetadataChips`/`CopyButton`/`SearchableSelect`/
  `SearchBar`/`ConnectionStatusBanner` and mis-located `ConfirmationModal`).
  **Browser-verified myself**: gallery page's sort dropdown opens correctly post-consolidation
  (screenshot confirms all 5 options render, current selection highlighted).
- **H3** — three in-document find implementations existed despite `lib/search/CLAUDE.md`'s
  explicit "don't add a third" warning. Confirmed the two LIVE ones
  (`TranscriptSearch.svelte`'s `computeMatches`, indexOf-based; `SearchTranscriptModal.svelte`'s
  `buildMatchPositions`, time-range-based) are genuinely different domains, not duplicates — so
  unifying them would be the wrong fix. Deleted the dead third (`lib/search/findInText.ts` +
  test, zero production callers), updated the CLAUDE.md warning to state the accurate current
  shape instead of resolving-in-place.
- **H4** — 5 duplicated constants: (a) 3 backoff implementations, one genuinely disagreeing
  (`multipartUploader.ts` used 500ms/uncapped/unjittered vs the shared `backoff.ts`'s
  1000ms/30s-cap/jittered) — repointed `uploadService.ts` and `multipartUploader.ts` to the
  shared implementation; **this is a real behavior change** for multipart part-retry timing,
  flagged explicitly, not hidden as "just a refactor"; (b) 4 raw `setTimeout` debounce
  reimplementations repointed to the shared `createDebouncedHandler`; (c)
  `RENAME_PROPAGATION_TIMEOUT_MS`/`PROPAGATION_TIMEOUT_MS` (same 20000ms value, two names, two
  files) consolidated to one export from `lib/api/speakerClusters.ts`; (d) hardcoded gallery
  page-size `'100'` in `routes/+page.svelte` now reads the actual store value; (e)
  `TranscriptSearch.svelte`'s cleanup timer (2000ms) now matches the CSS animation it's cleaning
  up after (1000ms, `search-pulse 1s`) instead of a mismatched value copied from a different,
  1.6s pairing 7 lines away in the same stylesheet.
- **H5** — no z-index scale existed; `BaseModal.svelte` disagreed with itself three ways (a
  `1300` prop default, a separate dead-and-unreachable `.modal-backdrop{z-index:1000}` CSS rule,
  and an independent `1300 !important` in a media query), and ~9 other files copy-pasted the
  bare `1300` literal. Added 8 documented tiers (`--z-base` through `--z-max`) to
  `styles/theme.css` with a migration-guide comment mapping the ~95 untouched raw values to the
  nearest tier (deliberately NOT migrating all of them today). Fixed `BaseModal`'s 3-way
  self-disagreement to reference one `--z-modal` custom property everywhere; repointed the 9
  bare-`1300` sites; pointed `ToastContainer.svelte` and `ClassificationBanner.svelte` (both
  `9999`, both bottom-anchored — a collision candidate the original analysis flagged as
  *unverified*) at the same `--z-toast` tier rather than guessing a stacking order between them.
  **Browser-verified myself**: opened the "About OpenTranscribe" `BaseModal` instance — renders
  correctly above all page content in both themes, no stacking regression from the `var()`
  refactor.
- Verified independently: `svelte-check` 0/0 (1216 files), `npm run test` 1591/1591 (165 files),
  `check:i18n` OK (5184×12). Read diffs for `apiError.ts`-adjacent CSS ordering, `BaseModal.svelte`
  (z-index resolution + dead-rule removal), `backoff.ts`/`multipartUploader.ts` (1-based vs
  0-based attempt-number conversion at the call site), `theme.css`'s new scale — all correct.

**Whole-group re-verification after all 3 batches** (my own, not any subagent's report):
`svelte-check` 0 errors/0 warnings (1216 files); `npm run test` 165 files / 1591 tests, all
passing; `npm run check:i18n` parity OK across all 12 locales; `npm run test:audit` 0 open
findings; backend `ruff`/`mypy` clean on `system.py` + its test; `python3 scripts/audit-tests.py
tests` 0 open findings (unchanged from baseline — this group barely touches backend).
**Live browser verification** (not delegated) via `~/bin/browser-tools/browse.js` against the
dev stack (`http://localhost:5173`, `admin@example.com`): logged in, exercised the gallery's
new `SortDropdown`, confirmed the app boots and navigates cleanly post-restart. Hit one real
issue along the way: a **stale Vite HMR error overlay** ("Identifier
'RENAME_PROPAGATION_TIMEOUT_MS' has already been declared") blocked the login button — the
underlying file was already correct (H4c's fix had already landed cleanly), so this was Vite's
dev-server error overlay persisting from an earlier, now-superseded intermediate edit state, not
a real bug. `./opentr.sh restart-frontend` cleared it; confirmed clean afterward. Filed nowhere
— not a code defect, a known class of Vite dev-server staleness this repo's own CLAUDE.md
already documents ("a split Vite module graph... a content-free touch fixed it").

## Group 8, batch 1: Backend dead-code deletions (E1, E3, E4, E6, E7)

Sonnet-delegated (highest-risk group of the day — deletions, not additions — split into 2
batches so each gets its own health gate before layering more risk on top). Every deletion
target was pre-verified dead by the plan's own `ast`-inventory audit; the subagent was told to
re-verify each one itself with a fresh untruncated `rg -n "\bNAME\b" backend` before deleting,
and to skip-and-report rather than guess on any discrepancy. It did exactly that once (see E6
below) instead of forcing a wrong deletion to match the plan's predicted count.

- **E1** — 3 dead functions each hiding behind a same-named LIVE twin with a DIFFERENT
  signature (the #542 camouflage pattern): `db_helpers.py::get_file_tags(db, file_id)` (live
  twin: `files/crud.py::get_file_tags(db, file_id, user_id)`), `direct_auth.py::verify_password`
  (live twin: `core/security.verify_password`), `lockout.py::record_successful_login(identifier)`
  (live twin: `auth/login.py::record_successful_login(db, user)`, different signature). Deleted
  all three plus the dead one's own dedicated test block (`test_db_helpers.py`, 4 tests — coverage
  for something nothing calls is camouflage, not coverage) and its `utils/README.md` mention.
- **E3** — 6 superseded implementations left beside their replacements: `prompt_manager.py`'s
  `get_prompt_for_content_type`/`create_user_prompt`/`set_user_active_prompt` (the live API
  reimplements all three inline instead of calling them), `uuid_helpers.py`'s
  `get_asr_config_by_uuid` (self-documented cross-agent scaffolding, `UserASRSettings` now exists
  and is used elsewhere) and `get_file_by_uuid_with_sharing` (superseded by the
  collection-level `PERMISSION_LEVELS` system), `api/websockets.py`'s `send_notification` /
  `broadcast_notification` (superseded by the Redis pub/sub fan-out — `send_ws_event` is
  documented as "THE way"). ⚠️ Verified the DIFFERENT, heavily-used Celery task also named
  `send_notification` in `tasks/watch_source_tasks.py` was never touched — `git diff` on that
  file shows zero changes.
- **E4** — deleted 2 self-labeled-DEPRECATED dead functions (`record_failed_attempt`,
  `is_account_locked`) plus 2 documented-but-never-routed admin functions
  (`get_all_locked_accounts`, `reset_lockout_count` — `admin.py` only ever imported
  `unlock_account`). Not a security hole (the atomic lockout-check path used everywhere that
  matters doesn't depend on either), just an unfinished admin surface. Decided to delete +
  drop the claim rather than build new admin routes today (building a feature is out of scope
  for a remediation pass) — filed
  **[#570](https://github.com/attevon-llc/OpenTranscribe/issues/570)** for it instead.
- **E6** — ~20 single-function orphans (each confirmed at exactly 1 `rg` hit — their own
  definition) across `password_history.py`, `core/security.py`, `core/auth_settings.py`,
  `core/settings_cache.py`, `db/session_utils.py` (+ doc fix in `app/db/README.md`),
  `services/llm_service.py`, `services/llm_bedrock.py` (module itself stays live —
  `stream_converse` keeps Bedrock reachable), `services/redaction/device.py`,
  `services/system_settings_service.py`, `transcription/engine/{audio_loader,progress}.py`,
  `utils/{client_ip,hardware_detection}.py` (+ doc fix in `docs/CROSS_PLATFORM_README.md`),
  `utils/{nvml_monitor,scratch_volume}.py`. **`core/exceptions.py` carve-out honored** — its
  module docstring explicitly forbids "finishing" this dedup pass, skipped entirely, untouched.
  **`encryption.py` discrepancy, correctly NOT forced**: the plan predicted 5 dead functions;
  the subagent's own sweep found only 4 genuinely dead (`test_legacy_compatibility`,
  `encrypt_if_not_empty`, `decrypt_if_not_empty`, `encrypt_value` — the dead write-side twin of
  the live `decrypt_value`, which stays). The 5th candidate near those line numbers,
  `test_encryption`, is live (called directly from 3 settings endpoints) — reported as a
  discrepancy rather than deleted to match a predicted count. Verified this myself: `git diff`
  on `encryption.py` shows exactly those 4 removed, `decrypt_value`/`test_encryption`/
  `generate_encryption_key` all present and untouched.
- **E7** — deleted the orphaned `formatting_service.py::format_file_size` (zero callers).
  Deduped `admin.py::format_bytes` / `tasks/utility.py::format_bytes` (byte-for-byte identical
  behavior, verified) into one canonical `utils/stats_helpers.py::format_bytes`, imported by
  both call sites; new test pins representative output values AND asserts both modules import
  the literal same function object (`admin_module.format_bytes is format_bytes`), so a future
  re-duplication would be caught by identity, not just by drift in behavior. Left
  `format_bytes_detailed` (the user-visible one, rendered in 4+ frontend components) completely
  untouched, per the plan's explicit warning that unifying its precision is a UI decision
  needing a browser check, not a cleanup.
- **Verification, independently re-run by me (not trusted from the subagent's report alone)**:
  `ruff check` + `mypy` clean across all 26 touched `app/*.py` files. Full fast suite
  (`pytest --ignore=tests/e2e -m "not integration and not gpu"`) run by me from scratch:
  **11271 passed, 2 failed, 68 skipped, 74 xfailed in 863.74s** — the 2 failures are the same
  pre-existing `test_env_template_sync.py` ones documented at the top of this file and in every
  group since Group 5; exact match to the subagent's own reported numbers, so its report is
  trustworthy here. `audit-tests.py`: 0 open findings (173 backlog, unchanged). **First health
  gate**: `./opentr.sh restart-backend` → clean boot, `/health` returns 200, no
  error/traceback/exception in the startup log. E5 and E8 (batch 2, more complex — E5 touches
  the frontend admin UI and adds a real validation fix, E8 needs a stated enumeration
  methodology before any deletion) come next, with their own second health gate before this
  group's final commit.

## Group 8, batch 2: E5 remainder + E8 schema cleanup

Discovered mid-batch-1 review that E5 was only 1/3 done by Group 4 (backend validation only —
`update_engine_settings` rejects an unknown `transcriber_backend`) — the admin dropdown removal
and the "reserved extension point" registry comment were still outstanding, contrary to my own
task-list note marking E5 "done". Checked `git diff` (which only shows uncommitted changes) for
the registry comment and wrongly concluded it was missing; it was actually already committed in
`ae1c0a46` and invisible to a diff for that reason — corrected once the first subagent's report
called it out explicitly.

**Two dispatches were needed.** The first subagent (E5 dropdown/i18n + E8 enumeration) ended its
turn mid-flow, apparently after starting a background verification command whose completion it
never saw — it reported "waiting for a notification" instead of a finished report. I checked the
working tree directly rather than trusting the incomplete summary: Task 1's frontend removal
(`EngineSettings.svelte`, 12 locale files, `settingsSearchIndex.test.ts`) was genuinely
complete and correct (verified by reading the diff myself), but Task 2 (E8) showed zero
`backend/app/schemas/` changes — not started. Resumed with a second, fully-self-contained
dispatch to finish exactly the two missing pieces. That second agent, while doing its own
independent E8 enumeration, discovered the FIRST agent's E8 work was actually already sitting in
the working tree (it must have completed Task 2 before its report got cut off) — diffed its own
independently-derived 30-class list against what was already there and found an **exact 1:1
match**, and correctly treated that as cross-validation rather than blindly re-applying (which
would only have risked a conflict with no benefit).

- **E5 remainder**: removed the `transcriber_backend` dropdown from `EngineSettings.svelte`
  (response interface field, draft state, load/save/isDirty wiring, the whole form-row block)
  — the `diarizer_backend` dropdown (which IS live) left completely untouched. Removed the
  now-dead `settings.engineSettings.transcriberBackend` i18n key from all 12 locales. Renamed 2
  now-invalid example rows in `settingsSearchIndex.test.ts` (a self-contained inline-mock test,
  not real i18n) rather than deleting them, since the test's generic search-index logic still
  needed 2 example rows to exercise. **Browser-verified myself**: opened Settings → Engine
  Configuration as `admin@example.com` — only "Diarizer Backend" renders now, no console
  errors, no visual regression.
- **E8**: enumerated dead Pydantic schemas via a stated, mechanical method (≤1 whole-repo `rg`
  hit AND not a base class of anything else in `backend/app/schemas/` via an AST
  `ClassDef.bases` walk AND not in any `__all__`/re-export) — **independently derived twice**,
  by two different subagent runs, both landing on the exact same **30 classes across 7 files**
  (`admin.py` ×2, `media.py` ×12, `search.py` ×8, `speaker_cluster.py` ×4, `topic.py` ×1,
  `user.py` ×2, `watch_source.py` ×1 — full table in the commit message). The first enumeration
  attempt found ZERO dead classes — caught and fixed a real methodology bug before trusting any
  count: `backend/mutants/` (mutmut's full source copy) and `.claude/worktrees/` (a live
  separate git worktree) were inflating every class's hit count by silently double- or
  triple-counting its own definition, so nothing ever looked unused. The known, pre-existing,
  documented dead set in `search.py` (`schemas/CLAUDE.md` already says the endpoint hand-builds
  a dict instead of using these) was correctly included in the mechanical enumeration rather
  than hand-waved as "already known" — 8 of that file's classes matched the method and were
  deleted alongside everything else; the other ~7 in the same file survive because they're used
  as nested field types by their still-referenced siblings, which is real usage under the
  stated method even though the endpoint itself never wires any of them in.
  One deletion introduced a bug, self-caught before I ever saw it: AST line-range deletion of
  `user.py`'s `BannerAcknowledgmentRequest` missed a trailing comment (comments aren't AST
  nodes, so `ClassDef.end_lineno` doesn't cover them), orphaning it inside the next class's
  method body. Found by systematically re-scanning all 30 deletion sites for the same pattern
  (exactly 1 hit) and fixed by hand.
  **Reported, not acted on** (correctly out of the single-pass method's scope): re-running the
  enumerator after the 30 deletions surfaces 5 NEW candidates whose only remaining reference was
  inside a class just deleted (`search.py`'s `EmbeddingModelSchema`/`NeuralModelInfoSchema`,
  `speaker_cluster.py`'s `SpeakerClusterMemberResponse`, `media.py`'s `SubtitleFormat` enum,
  `topic.py`'s `TopicSuggestionBase`, the former base of the now-deleted
  `TopicSuggestionCreate`). This is a legitimate second-order cleanup, deliberately left for
  Phase B's re-audit loop rather than chased in the same pass — "delete exactly the enumerated
  list, nothing more" was the stated discipline for a reason: a moving target during the same
  commit is how a deletion pass loses track of what it actually verified.
- **Verification, independently re-run by me end-to-end after both agents**: `ruff check` +
  `mypy` clean on `app/schemas/` (39 files) and `app/transcription/engine/backends/`.
  `svelte-check` 0/0 (1216 files), `npm run check:i18n` OK (5183×12). Full fast suite run by me
  from scratch a second time (not trusted from either subagent alone, given the first one's
  incomplete report): **11271 passed, 2 failed (the same pre-existing
  `test_env_template_sync.py` pair), 68 skipped, 74 xfailed in 855.08s** — byte-for-byte the
  same counts as batch 1's run, confirming zero regressions from either E5 or E8.
  `audit-tests.py`: 0 open findings. **Second health gate**: `./opentr.sh restart-backend` +
  `restart-frontend` → both healthy, `/health` 200, no errors in the startup log, `/` returns
  200. **Live browser verification** (not delegated): logged in, opened Settings → Engine
  Configuration, confirmed the dropdown is gone and the panel renders with zero console errors.

### Group 9 — Docs (L1-L8)

- **L3/L4/L5** — confirmed already satisfied by earlier groups' own doc updates; no separate
  action needed.
- **L1** (done directly) — deleted the stale `endpoints/tags_pkg/` dead-code warning bullet from
  `backend/app/api/CLAUDE.md`; confirmed the directory doesn't exist.
- **L2** (done directly) — `backend/app/tasks/CLAUDE.md` claimed "`visibility_timeout` is NOT
  configured anywhere in this repo" and that duplicate-transcription redelivery was real and
  unfixed. Group 6's `.env.example` work (K3b) had already added `CELERY_VISIBILITY_TIMEOUT`
  and wired it into `core/celery.py`'s `broker_transport_options`, making the warning stale and
  actively misleading (it read as an open bug that was already closed). Rewrote it to name the
  fix and the single source of truth. Carried the same correction into project memory
  (`project_improvements_security_review.md`'s "Verified reliability hazards" line — struck
  through with a dated correction, rest of the line's still-valid findings left untouched).
- **L6** (done directly) — `backend/app/schemas/CLAUDE.md`'s dead-schema bullet, written before
  E8 ran, named 4 classes (`CustomVocabularyResponse`, `UserASRSettingsResponse`,
  `ASRSettingsList`, `SpeakerClusterResponse`) as dead. None of them were in E8's actual
  30-class deletion list — verified via `rg` that all 4 are re-exported through
  `schemas/__init__.py`'s `__all__` (E8's method explicitly excludes re-exports as real usage),
  and two are additionally referenced as nested field types in their own files. Rewrote the
  bullet to name the real 8 deleted `search.py` classes, explain why the other 8 `search.py`
  classes legitimately survive, explicitly correct the 4 misnamed classes with the actual
  reason they're alive, and flag E8's 5 second-order orphans (already noted above) as deferred
  to Phase B rather than implying the list is now exhaustive.
- **L7** (delegated, Sonnet) — doc↔code consistency sweep, scoped to the mechanically-verifiable
  half only (every `./opentr.sh`/`npm run`/script/path reference in a `.md` file must resolve);
  explicitly out of scope: full feature-reachability auditing, which is subjective and belongs
  to a future pass. Swept all 225 `.md` files in the doc surface (root + 68 nested `CLAUDE.md` +
  `docs/` + `docs-site/docs/` + top-level READMEs), ~83 distinct script paths, every
  `./opentr.sh <subcommand>`/`--with-*` flag, every `npm run` script, and all 441 relative
  markdown links. Found and fixed real drift in 20 files (no commits made by the agent — I
  reviewed and staged everything myself):
  - **4 nonexistent `opentr.sh` subcommands**, referenced across 8 files: `restart` (renamed to
    `restart-all`; two call sites also had bogus `restart backend`/`restart opensearch`
    service-arg forms), `start-backend` (→ `restart-backend`), `init-db` (removed; migrations
    now run automatically on dev backend startup), `clean` (removed with no safe non-destructive
    equivalent — replaced with the working `docker system prune -a` example already used
    elsewhere in the same doc).
  - **2 nonexistent verification scripts**: `./scripts/verify-fips-140-3.sh` (repointed to the
    real mechanism, `RUN_FIPS_TESTS=true pytest tests/test_fips_140_3.py`); left
    `database/prompt_improvements.sql`/`verify-fips.sh` alone where the doc explicitly presents
    them as templates to create, not existing files.
  - **1 stale npm script**: `npm run format` doesn't exist (prettier only runs via pre-commit) —
    `docs/CONTRIBUTING.md` fixed to `npm run lint:fix`.
  - **Renamed/refactored source paths still documented under old names**: `opensearch_service.py`
    → now a package `opensearch_service/` (2 files); `fast_speaker_assignment.py` (interval-tree)
    → superseded by `backend/app/transcription/speaker_assigner.py` (vectorized numpy) in
    `TRANSCRIPTION_OPTIMIZATION.md` — corrected the path AND the now-inaccurate "273x speedup"
    claim to the current numbers, verified against the module's own docstring (~13x, 80s→6s for
    a 4.7hr/54K-word file) rather than invented; a nonexistent
    `download_quality_service.py` line removed (quality selection is already correctly
    attributed to `media_download_service.py` in the same doc); a nonexistent frontend
    `ActionBox` component removed from the architecture/contributing docs' UI catalog, and
    `ids.ts`'s path corrected to its real location.
  - **Systematic relative-link breakage** (off-by-one directory level): `BACKEND_DOCUMENTATION.md`
    (60 links), `CONTRIBUTING.md` (3), `DOCKER_DEPLOYMENT.md` (6), plus one spurious extra
    `docs/` prefix in `INSTALLATION.md` and a dead `LLM_DEPLOYMENT_OPTIONS.md` link in `README.md`
    repointed to the current `docs-site/docs/features/llm-integration.md`.
  - Correctly left alone: `feat/doc-ingestion`-branch forward references (verified against that
    branch's actual Alembic head via `alembic-head.py`, not guessed), explicitly-marked
    "planned"/"to come" research docs, a different project's script names in a comparison doc,
    and `--with-saml-test` (both mentions correctly state the flag doesn't exist yet).
  - **Reverse-check finding, not acted on** (documentation gap, not drift — out of L7's fix
    scope): `scripts/opentr-offline.sh`, `build-windows-installer.sh`,
    `diagnose-file-integrity.sh`, `bulk-processing-cheatsheet.sh`, `fix-shared-volume-perms.sh`
    are user-facing scripts with zero doc mentions anywhere. Worth a future look, `opentr-offline.sh`
    especially given `README-OFFLINE.md` already exists.
- **L8** — confirmed it needs no action; the plan's own text says the row exists only so the
  doc-consistency sweep doesn't get reported twice, not as a separate task.
- **Verification**: this is a pure-doc change (24 files: my 3 direct `CLAUDE.md` edits + the
  agent's 21 `.md` fixes) — no code paths touched, so no backend/frontend test run is
  applicable. Ran the full `scripts/safe-precommit.sh run --all-files` gate before committing
  (markdownlint/prettier/whitespace hooks apply to `.md`; nothing else in this group could
  trip ruff/mypy/svelte-check since zero `.py`/`.ts`/`.svelte` files changed).

### Group 10 — Tests, J1-J3 (delegated, Sonnet; J4 still pending, Opus-mandated)

54 findings repaired: 16 J1 (`no-assertion` ×11, `conditional-only` ×3, `self-comparison` ×1,
`bare-return` ×1), 12 J2 (`negated-status` ×8, `permissive-status` ×3,
`status-guarded-assert` ×1), 26 J3 (`loop-only`). `audit-tests.py tests`:
**0 open, 119 backlog (down from 173), 13 accepted (up from 10)** — independently re-run and
confirmed by me, not just trusted from the agent's report. Selftest 76/76.

- **J1 removals/conversions, verified by reading the diff**: `e2e/test_speaker_sharing.py
  ::test_final_screenshot` deleted outright (its body was one `.screenshot()` call, no
  assertion — `test_visual_regression.py` already covers `/speakers` properly).
  `e2e/test_a11y.py::test_baseline_is_current` converted from a self-overwriting non-test
  (`pytest.skip` unless `UPDATE_A11Y_BASELINE=1`, then rewrote the very baseline it claimed to
  verify) into a real test asserting exact set-equality between the committed baseline and
  observed rule IDs in both directions (catches a regression AND a stale baseline entry that's
  quietly stopped meaning anything). The write path moved to a new standalone
  `scripts/update-a11y-baseline.py`; shared scan/baseline logic extracted to new
  `backend/tests/e2e/a11y_lib.py`. Confirmed the new test's reliance on definition-order
  execution (three page-scan tests populate a module-scoped accumulator before the final
  assertion reads it) is safe under the real E2E runner: `scripts/e2e/run-e2e.sh` invokes
  pytest-xdist with `--dist loadfile`, which keeps every test in one file on the same worker —
  pytest-randomly is not installed, so within-file order is preserved.
- **J1 self-comparison rewrite, verified**: `unit/test_chat_retrieval.py
  ::test_cache_key_is_stable_within_one_corpus_version` no longer compares a literal call to
  itself (`_key_with_corpus("7") == _key_with_corpus("7")`, true regardless of whether the
  corpus version is even part of the key). Rewritten to build the same scope two different
  orders via the real `scope_hash()` helper and assert both resulting `cache_key()`s agree —
  a real determinism+ordering check. Caught one thing the agent's own report didn't flag:
  the rewrite originally splatted a `**dict[str, str | int | None]` into `cache_key()`'s
  narrowly-typed keyword-only params, which mypy correctly rejected (8 errors) since a dict's
  value type can't distribute across per-parameter types — fixed by passing each keyword
  explicitly on both calls instead of splatting a shared dict. Re-ran the 5
  `test_cache_key_*` tests after the fix: all pass.
- **J2 `test_tenant_isolation.py` exception, verified**: the plan explicitly calls out this one
  test as the wrong place for a blanket "pin the exact status" fix, since its own docstring
  states the invariant is deliberately status-agnostic apart from never being 403. Confirmed
  the fix kept `!= 403` and added `< 500` as a second assertion rather than pinning one status,
  and confirmed the allowlist entry was correctly moved from BACKLOG to ACCEPTED with a written
  reason (not silently dropped — the pattern is intentional and permanent, not a repair
  target).
- **Independent verification performed by me** (not just trusted from the report): re-ran
  `audit-tests.py tests` + `--selftest`, re-diffed every touched file, re-ran the specific
  `test_cache_key_*` tests after my own mypy fix, and ran the full
  `scripts/safe-precommit.sh run --all-files` gate twice (once catching the mypy issue above,
  once clean after the fix) — ruff/ruff-format auto-fixed import ordering + formatting on 4
  files in the first pass, re-verified clean on the second. No app code touched (test files +
  2 new scripts only), so no health-gate restart was needed for this group.
- **Agent's own execution-verified testing** (588 backend unit/api tests across all touched
  files pass; 8 E2E files run live against the dev stack, all green after fixing one flaky
  race caught mid-run; `test_search_quality.py`'s one failure confirmed pre-existing corpus
  gap — 5 other untouched tests in the same file fail identically, and that suite is
  documented as deliberately excluded from CI). Two things verified by reading/reasoning only,
  not execution: `test_auth_buttons.py::test_pki_button_click_attempts_auth` (PKI not enabled
  on this dev stack) and `test_embedding_migration_integration.py::test_throughput_benchmark`
  (needs GPU model loading — not run, per the standing GPU-safety rule against unscheduled
  GPU batches).
- **J4 (92 findings, three-way triage split) deliberately NOT done in this pass** — per the
  plan, this tier needs careful per-finding judgment (repair ~27 genuine gaps, move ~23 to
  ACCEPTED with written reasons, leave the rest BACKLOG) and is one of the four tasks this
  effort mandated for Opus delegation specifically. Separate dispatch, next.

### Group 10 — Tests, J4 (delegated, Opus)

98 findings remained after excluding J1-J3's cleared categories and J5 (a later, separate
tier — `error-swallowed`/`broad-raises`/`sleep-sync`, left untouched): `weak-only` 41,
`mock-only` 51, plus `failure-masking` 3 and `readiness-probe-target` 3, both correctly left
alone (the former shares its 3 tests with J5's `error-swallowed` findings — fixing one half
without the other is worse than neither; the latter already carries a precise, correct
BACKLOG diagnosis, not an UNTRIAGED placeholder). J4's real scope was the predicted **92**
(`weak-only` + `mock-only`). `audit-tests.py tests`: **0 open, 119 → 34 backlog, 13 → 54
accepted**. Selftest 76/76 — independently re-run and confirmed by me.

**The actual split: 44 repaired, 41 accepted, 7 left backlogged** — versus the plan's
~27/~23/~42 estimate. Read the reasoning for the shift (it's not corner-cutting): 10 of the
44 repairs came from `mock-only`, not just `weak-only` — the 7 `test_auth_config_service.py`
encryption-at-rest cases turned out genuinely repairable (`assert_called_once_with` on the
cipher call, never checking the STORED value — proven live: a `git archive HEAD` tree
mutated to encrypt-then-store-plaintext-anyway passed the OLD assertions 2/2 and failed the
NEW ones). And `mock-only` turned out far more uniformly legitimate than a sample suggested:
of 51 read in full, 41 are functions returning `None` whose entire behavior IS the outgoing
call (dispatchers, audit emissions, `assert_not_called()` on destructive OpenSearch deletes,
the exact query body carrying `accessible_user_ids`) — no return value exists to assert
instead, so each got an individually-written ACCEPTED reason, not shared boilerplate.
Confirmed via `grep`: 0 `mock-only` lines still say BACKLOG; all 41 correctly moved. The 7
left BACKLOG are the ones that could not be verified, each with real per-test reasoning
instead of "UNTRIAGED" (spot-checked all 7 myself): 3× `test_pki.py` (needs the prod+nginx
PKI overlay), `test_ldap_login_shows_gallery` + `test_mfa_login_shows_code_prompt` (need
LLDAP / an MFA-enrolled account), the `gpu`-marked `test_model_manager_sequential_handoff`
(plausibly an ACCEPT but unconfirmable without the GPU worker), and
`test_every_category_has_keys_to_filter_on` (every available tightening is tautological —
`CONFIG_CATEGORIES` is a comprehension over `CATEGORY_SCHEMAS`, so pinning the derived value
just restates the comprehension; needs an independent source of truth, a small design
question, not a mechanical edit — correctly not forced).
- **Repairs verified by me, spot-check on the trickiest**: `test_auth_config_service.py`'s
  7 cases (read in full — each now asserts the built row's actual fields, e.g. `is
  ["encrypted"]`/`!= ["secret_value"]`/`is True`, not `add.call_count`), `test_mfa_security.py`
  (`verify_backup_code`'s matched-hash return, previously discarded to `_`, now asserted so
  a code the caller fails to invalidate can't be replayed; `test_totp_window_zero_only_
  accepts_current` now asserts the refused halves — previous/next 30s-step codes — pinned
  against one fixed `now` so it can't be timing-flaky). Ran the 40-test file after: all pass.
- **The one unexecuted repair, flagged honestly rather than silently included**:
  `test_ldap_oidc.py::test_ldap_wrong_password_rejected` mirrors the already-green
  `test_login.py::test_wrong_password` shape exactly, but needs LLDAP running to execute —
  starting it restarts the shared stack, out of scope for this pass. Verified independently:
  both `re` and `expect` (playwright) are already imported in the file, and the file parses
  clean (`ast.parse`). Needs a real `RUN_AUTH_E2E=true` run before being trusted blind;
  tracked as a follow-up, not silently claimed as tested.
- **One real bug I found and fixed myself, not caught by the agent's own report**: the new
  `_added()`/`_audit_row()` test helpers in `test_auth_config_service.py` were untyped, so
  mypy correctly flagged `_audit_row`'s declared `-> AuthConfigAudit` return as `Any` (the
  isinstance-filtered list has no type the checker can follow through). Fixed properly —
  not suppressed — with a PEP 695 generic (`def _added[M](mock_db, model: type[M]) ->
  list[M]`, this repo's Python 3.12+ target syntax; ruff's `UP047` rejected a
  `TypeVar`-based first attempt). Re-ran the 40-test file and the full gate after: clean.
- Full `scripts/safe-precommit.sh run --all-files` gate run 4 times total across J1-J3 and
  J4 (2 real findings caught and fixed on J4's first pass: the mypy issue above, plus ruff's
  auto-fixed import ordering) — clean on the final pass. No app code touched — test files +
  the allowlist only, so no health-gate restart needed.
- Group 10 now fully closes out J1 through J4. J5 (error handling in tests, 21 findings) and
  J6-J8 (new tests per fix, missing coverage, allowlist-reason audit) remain explicitly out
  of this effort's scope per the original plan — not started, not implied done.
